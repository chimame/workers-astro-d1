services:

  app:
    build:
      context: .
    entrypoint: /app/scripts/docker-entrypoint.sh
    command: pnpm run dev --host 0.0.0.0
    volumes:
      - .:/app:cached
    ports:
      - 3000:3000
    tty: false
    stdin_open: true
